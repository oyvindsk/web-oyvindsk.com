# `gcloud build submit` will ignore (leave out) these as well, by default!
*.log
*SECRET*

# Binary files:
asciidoc-runner/asciidoc-runner


# Ignore files generated by ./asciidoc-runner 
# for now.. will probably want them in the repo when things are more stable 

## echo -e "\n" >> .gitignore 
## ./asciidoc-runner/find-files-to-cleanup.sh blogbackend/ >> .gitignore
